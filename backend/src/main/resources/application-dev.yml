spring:
  config:
    activate:
      on-profile: dev
  application:
    name: perguntinhas
  datasource:
    url: jdbc:postgresql://localhost:5432/perguntinhas
    username: postgres
    password: postgres
  flyway:
    url: jdbc:postgresql://localhost:5432/perguntinhas
    user: postgres
    password: postgres
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: none